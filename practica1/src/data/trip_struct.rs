#[derive(Debug, Clone)]
pub struct Trip {
    pub index: String,
    pub pickup_datetime: String,
    pub dropoff_datetime: String,
    pub total_amount: String,
    pub passengers: String,
    pub distance: String,
}
